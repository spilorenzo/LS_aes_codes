//
//  AES.c
//  AES_implementation
//
//  Created by Lorenzo Spignoli on 07/01/2021.
//  Copyright © 2021 Lorenzo Spignoli. All rights reserved.
//

#include "AES.h"

byte sbox[256]={
};

byte rsbox[256]={
};

byte rcon[255]={
};

// declare vector for the ExpandedKey
byte W[4*Nk*(Nr+1)];

void initial_state(state vct_state, byte *input){
    
    // each element of input is copied in output
    for (int i=0; i<4*Nb; i++) {
        vct_state[i] = input[i];
    }
    
};

void final_output(state vct_state, byte *output){
    
    // each element of the state is copied in a string
    for (int i=0; i<4*Nb; i++) {
        output[i] = vct_state[i];
    }
};

void print_state(state vct_state){
    
    // print the state in matrix form
    for (int i=0; i<4; i++) {
        for (int j=0; j<Nb; j++) {
            printf("%X ", vct_state[i + 4*j]);
        }
        printf("\n");
    }
};

void print_expandedkey(){

    // print the ExpandedKey in matrix form
    for (int i=0; i<Nr+1; i++) {
        printf("Round %d: ",i);
        for (int j=0; j<16; j++) {
            printf("%X ", W[(i*16)+j]);
        }
        printf("\n");
    }
}

void key_expansion(byte *key){
    
    // define auxiliary vector for column of W
    byte temp[4];
    
    // the first RoundKey is the key itself
    for (int i=0; i<4*Nk; i++){
        W[i] = key[i];
    }
    
    // compute the other RoundKeys
    for (int i=4*Nk; i<4*Nb*(Nr+1); i+=4) {
        if (i%(4*Nk) == 0) {
            // recover the 4th previous column
            temp[0] = W[i-16];
            temp[1] = W[i-15];
            temp[2] = W[i-14];
            temp[3] = W[i-13];
            
            // xor with the sbox value of the priviuos column rotated
            temp[0] = temp[0] ^ get_sbox_value(W[i-3]);
            temp[1] = temp[1] ^ get_sbox_value(W[i-2]);
            temp[2] = temp[2] ^ get_sbox_value(W[i-1]);
            temp[3] = temp[3] ^ get_sbox_value(W[i-4]);
            
            // xor the first element of the column with RC[i/16]
            temp[0] = temp[0] ^ get_rcon_value(i/16);
            
            // save the computed column in W
            W[i] = temp[0];
            W[i+1] = temp[1];
            W[i+2] = temp[2];
            W[i+3] = temp[3];
            
        }
        else{
            // recover the 4th previous column
            temp[0] = W[i-16];
            temp[1] = W[i-15];
            temp[2] = W[i-14];
            temp[3] = W[i-13];
            
            // xor with the previous column
            temp[0] = temp[0] ^ W[i-4];
            temp[1] = temp[1] ^ W[i-3];
            temp[2] = temp[2] ^ W[i-2];
            temp[3] = temp[3] ^ W[i-1];
            
            // save the computed column in W
            W[i] = temp[0];
            W[i+1] = temp[1];
            W[i+2] = temp[2];
            W[i+3] = temp[3];
        }
    }
};

byte get_sbox_value(byte index){
    
    // return the index-th element of the SBox
    
    return sbox[index];
};

byte get_rsbox_value(byte index){
    
    // return the index-th element of the InvSBox
    
    return rsbox[index];
};

byte get_rcon_value(byte i){
    
    // return x^(i-1)
    
    return rcon[i];
};

void subbyte(state vct_state){
    
    for (int i=0; i<4*Nb; i++) {
        byte temp = vct_state[i];
        vct_state[i] = get_sbox_value(temp);
    }
};

void inv_subbyte(state vct_state){
    
    for (int i=0; i<4*Nb; i++) {
        
        byte temp = vct_state[i];
        vct_state[i] = get_rsbox_value(temp);
    }
};

void shiftrows(state vct_state){
    
    // define auxiliary variable for the shifting
    byte temp;
    
    // the 0th row remain the same, so shift the 1st row by one column position
    temp = vct_state[1];
    vct_state[1] = vct_state[5];
    vct_state[5] = vct_state[9];
    vct_state[9] = vct_state[13];
    vct_state[13] = temp;
    
    // shift the 2nd row by two column positions
    temp = vct_state[2];
    vct_state[2] = vct_state[10];
    vct_state[10] = temp;
    temp = vct_state[6];
    vct_state[6] = vct_state[14];
    vct_state[14] = temp;
    
    // shift the 3rd row by three column positions
    temp = vct_state[3];
    vct_state[3] = vct_state[15];
    vct_state[15] = vct_state[11];
    vct_state[11] = vct_state[7];
    vct_state[7] = temp;
};

void inv_shiftrows(state vct_state){
    
    // define auxiliary variable for the shifting
    byte temp;
    
    // the 0th row remain the same, so shift the 1st row by one column position
    temp = vct_state[1];
    vct_state[1] = vct_state[13];
    vct_state[13] = vct_state[9];
    vct_state[9] = vct_state[5];
    vct_state[5] = temp;
    
    // shift the 2nd row by two column positions
    temp = vct_state[2];
    vct_state[2] = vct_state[10];
    vct_state[10] = temp;
    temp = vct_state[6];
    vct_state[6] = vct_state[14];
    vct_state[14] = temp;
    
    // shift the 3rd row by three column positions
    temp = vct_state[3];
    vct_state[3] = vct_state[7];
    vct_state[7] = vct_state[11];
    vct_state[11] = vct_state[15];
    vct_state[15] = temp;
};

byte xtime(byte x){
    
    // compute the moltiplication by x (02)
        
    return ((x<<1) ^ (((x>>7) & 1) * 0x1b));
};

byte multiply(byte x, byte y){
    
    // compute the moltiplication between polynomials x * y mod x^4+1
    // taken from https://github.com/kokke/tiny-AES-c
        
        
    return (((y & 1) * x) ^
       ((y>>1 & 1) * xtime(x)) ^
       ((y>>2 & 1) * xtime(xtime(x))) ^
       ((y>>3 & 1) * xtime(xtime(xtime(x)))) ^
       ((y>>4 & 1) * xtime(xtime(xtime(xtime(x))))));
};

void mixcolumns(state vct_state){
    
    // define auxiliary variables for the multiplication
    byte t;
    byte u;
    byte v;
    
    byte temp[4];
    
    // for each column perform the multiplication
    for (int i=0; i<Nb; i++) {
        
        // consider the i-th colum
        temp[0] = vct_state[4*i];
        temp[1] = vct_state[(4*i)+1];
        temp[2] = vct_state[(4*i)+2];
        temp[3] = vct_state[(4*i)+3];
        
        // efficent computation of MixColumn as describe in AES textbook
        t = temp[0] ^ temp[1] ^ temp[2] ^ temp[3];
        u = temp[0];
        v = temp[0] ^ temp[1];
        v = xtime(v);
        vct_state[4*i] = vct_state[4*i] ^ v ^ t;
        v = temp[1] ^ temp[2];
        v = xtime(v);
        vct_state[(4*i)+1] = vct_state[(4*i)+1] ^ v ^ t;
        v = temp[2] ^ temp[3];
        v = xtime(v);
        vct_state[(4*i)+2] = vct_state[(4*i)+2] ^ v ^ t;
        v = temp[3] ^ u;
        v = xtime(v);
        vct_state[(4*i)+3] = vct_state[(4*i)+3] ^ v ^ t;
    }
};

void mixcolumns_slow(state vct_state){
    
    // define auxiliary variable for the column
    byte temp[4];
    
    // for each column perform the multiplication
    for (int i=0; i<Nb; i++) {
        
        // consider the i-th colum
        temp[0] = vct_state[4*i];
        temp[1] = vct_state[(4*i)+1];
        temp[2] = vct_state[(4*i)+2];
        temp[3] = vct_state[(4*i)+3];

        // moltiplication item by item
        vct_state[4*i] = multiply(temp[0], 0x02) ^ multiply(temp[1], 0x03) ^ multiply(temp[2], 0x01) ^ multiply(temp[3], 0x01);
        vct_state[(4*i)+1] = multiply(temp[0], 0x01) ^ multiply(temp[1], 0x02) ^ multiply(temp[2], 0x03) ^ multiply(temp[3], 0x01);
        vct_state[(4*i)+2] = multiply(temp[0], 0x01) ^ multiply(temp[1], 0x01) ^ multiply(temp[2], 0x02) ^ multiply(temp[3], 0x03);
        vct_state[(4*i)+3] = multiply(temp[0], 0x03) ^ multiply(temp[1], 0x01) ^ multiply(temp[2], 0x01) ^ multiply(temp[3], 0x02);
    }

};

void preprocessing_step(state vct_state){
    
    // define auxiliary variables
    byte u;
    byte v;
    
    byte temp[4];
    
    // for each column perform the multiplication
    for (int i=0; i<Nb; i++) {
        
        // consider the i-th colum
        temp[0] = vct_state[4*i];
        temp[1] = vct_state[(4*i)+1];
        temp[2] = vct_state[(4*i)+2];
        temp[3] = vct_state[(4*i)+3];
        
        byte aux_u = temp[0] ^ temp[2];
        u = xtime(xtime(aux_u));
        byte aux_v = temp[1] ^ temp[3];
        v = xtime(xtime(aux_v));
        
        
        vct_state[4*i] = vct_state[4*i] ^ u;
        vct_state[(4*i)+1] = vct_state[(4*i)+1] ^ v;
        vct_state[(4*i)+2] = vct_state[(4*i)+2] ^ u;
        vct_state[(4*i)+3] = vct_state[(4*i)+3] ^ v;
    }
};

void inv_mixcolumns(state vct_state){
    
    // efficent computation of InvMixColumn as describe in AES textbook
    // i.e. it is composed by a preprocessing step followed by a MixColumn
    preprocessing_step(vct_state);
    mixcolumns(vct_state);
    
};

void inv_mixcolumns_slow(state vct_state){
    
    // define auxiliary variable for the column
    byte temp[4];
    
    // for each column perform the multiplication
    for(int i=0; i<Nb; i++){
        
        // consider the i-th colum
        temp[0] = vct_state[4*i];
        temp[1] = vct_state[(4*i)+1];
        temp[2] = vct_state[(4*i)+2];
        temp[3] = vct_state[(4*i)+3];

        // moltiplication item by item
        vct_state[4*i] = multiply(temp[0], 0x0e) ^ multiply(temp[1], 0x0b) ^ multiply(temp[2], 0x0d) ^ multiply(temp[3], 0x09);
        vct_state[(4*i)+1] = multiply(temp[0], 0x09) ^ multiply(temp[1], 0x0e) ^ multiply(temp[2], 0x0b) ^ multiply(temp[3], 0x0d);
        vct_state[(4*i)+2] = multiply(temp[0], 0x0d) ^ multiply(temp[1], 0x09) ^ multiply(temp[2], 0x0e) ^ multiply(temp[3], 0x0b);
        vct_state[(4*i)+3] = multiply(temp[0], 0x0b) ^ multiply(temp[1], 0x0d) ^ multiply(temp[2], 0x09) ^ multiply(temp[3], 0x0e);
    }

};

void addroundkey(state vct_state, byte round){
    
    // compute element by element the xor between the state and the round key
    for (int i=0; i<4*Nb; i++) {
        vct_state[i] = vct_state[i] ^ W[(16*round)+i];
    }
    
};

byte *AES_encrypting(byte *plaintext, byte *key){
    
    // define the plaintex (block) as the initial state of AES
    state AES_state;
    initial_state(AES_state, plaintext);
    
    // expand the key
    key_expansion(key);
    
    // perform the first AddRoundKey
    addroundkey(AES_state, 0);
    
    // perform the Nr-1 rounds of the algorithm
    for (int i=1; i<Nr; i++) {
        
        // compute SubByte
        subbyte(AES_state);
        // compute ShiftRows
        shiftrows(AES_state);
        // compute MixColumn
        mixcolumns(AES_state);
        // compute AddRoundKey
        addroundkey(AES_state, i);
    }
    
    // perform the last round
    
    //compute SubByte
    subbyte(AES_state);
    // compute ShiftRows
    shiftrows(AES_state);
    // compute AddRoundKey
    addroundkey(AES_state, 10);
    
    // transform the chiphertext in a string
    byte *chiphertext = malloc(16*sizeof(byte));
    final_output(AES_state, chiphertext);
    
    return chiphertext;
};

byte *AES_decrypting(byte *chiphertext, byte *key){
    
    // define the chiphertext (block) as the initial state of InvAES
    state AES_state;
    initial_state(AES_state, chiphertext);
    
    // expand the key
    key_expansion(key);
    
    // perform the last AddRoundKey
    addroundkey(AES_state, Nr);
    
    // perform the Nr-1 rounds of the algorithm
    for (int i=Nr-1; i>0; i--) {
        // compute InvShiftRows
        inv_shiftrows(AES_state);
        // compute InvSubByte
        inv_subbyte(AES_state);
        // compute AddRoundKey
        addroundkey(AES_state, i);
        // compute InvMixColumn
        inv_mixcolumns(AES_state);
    }
    
    // perform the first round
    // compute InvShiftRows
    inv_shiftrows(AES_state);
    // compute InvSubByte
    inv_subbyte(AES_state);
    // compute AddRoundKey
    addroundkey(AES_state, 0);
    
    // transform the chiphertext as a string
    byte *plaintext = malloc(16*sizeof(byte));
    final_output(AES_state, plaintext);
    
    return plaintext;
};

int main2(int argc, char *argv[]) {
    
    srand((unsigned int) time(0));
    
    // define the plaintext (block) as a string
    //byte plaintext[BLOCKLEN] = {0x32,0x43,0xf6,0xa8,0x88,0x5a,0x30,0x8d,0x31,0x31,0x98,0xa2,0xe0,0x37,0x07,0x34};
    byte plaintext[BLOCKLEN] = {0x6b,0xc1,0xbe,0xe2,0x2e,0x40,0x9f,0x96,0xe9,0x3d,0x7e,0x11,0x73,0x93,0x17,0x2a};
    //byte plaintext[BLOCKLEN] = {0x54,0x77,0x6F,0x20,0x4F,0x6E,0x65,0x20,0x4E,0x69,0x6E,0x65,0x20,0x54,0x77,0x6F};
    //byte plaintext[BLOCKLEN] = {0x49,0x20,0x61,0x6d,0x20,0x73,0x6f,0x72,0x72,0x79,0x20,0x4a,0x75,0x6c,0x69,0x61};
    
    // define the key as a string
    byte key[KEYLEN] = {0x2b,0x7e,0x15,0x16,0x28,0xae,0xd2,0xa6,0xab,0xf7,0x15,0x88,0x09,0xcf,0x4f,0x3c};
    //byte key[KEYLEN] = {0x54,0x68,0x61,0x74,0x73,0x20,0x6D,0x79,0x20,0x4B,0x75,0x6E,0x67,0x20,0x46,0x75};
    
    // disply the plaintex and the key
    printf("Plaintext:\n");
    for (int i=0; i<BLOCKLEN; i++) {
        printf("%X ", plaintext[i]);
        if(i==BLOCKLEN-1){
            printf("\n");
        }
    }

    printf("Key in string form:\n");
    for (int i=0; i<KEYLEN; i++) {
        printf("%X ", key[i]);
        if(i==KEYLEN-1){
            printf("\n");
        }
    }
    
    byte *chiphertext = AES_encrypting(plaintext, key);
    
    printf("Chiphertext in string form:\n");
    for (int i=0; i<BLOCKLEN; i++) {
        printf("%X ", chiphertext[i]);
    }
    printf("\n");
    
    byte *test = AES_decrypting(chiphertext, key);
    
    printf("Decrypted chiphertext in string form:\n");
    for (int i=0; i<BLOCKLEN; i++) {
        printf("%X ", test[i]);
    }
    printf("\n");
    
    return 0;
}
